RONDA ONE 0.7.5 — CAROUSEL STABILITY + DESIGN HANDOFF

CORREÇÕES PRINCIPAIS

1. O frontend não encerra mais a geração aos 75 segundos.
   - 75 s passa a ser apenas um aviso de processamento prolongado.
   - o polling continua por até 8 minutos.
   - oscilações temporárias de rede são toleradas.
   - o job continua na Cloudflare Queue sem exigir F5.

2. RONDA DESIGN não depende mais do botão "Copiar roteiro".
   - se existem slides gerados, o botão RONDA DESIGN é habilitado.
   - um carrossel que ainda exige revisão editorial pode ser aberto no Design.
   - o projeto recebe editorialReviewRequired=true para preservar a condição de revisão.

3. O handoff passa a preservar:
   - editorialGate
   - facts
   - verificationLinks
   - reading
   - editorialStatus

4. Cache bust da integração:
   - ronda-one-integration.js?v=0.7.5
   - ronda-one-shell.css?v=0.7.5

INSTALAÇÃO

Envie o conteúdo deste ZIP para a raiz do repositório rondaone, substituindo os arquivos existentes.
NÃO altere DB, AI, ROUND_JOBS_QUEUE, INTELLIGENT_JOBS_QUEUE ou as filas já configuradas.

TESTES

/api/platform/status
Esperado: version 0.7.5 e carouselStability.clientPolling = resilient

Fluxo:
Ronda > assunto > Gerar roteiro
- após 75 s não deve aparecer erro terminal;
- a mensagem deve informar que o job continua na fila;
- ao concluir, RONDA DESIGN deve ficar disponível mesmo se Copiar roteiro continuar bloqueado para revisão.
