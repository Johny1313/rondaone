import assert from "node:assert/strict";import fs from "node:fs";const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');const e=read('src/production/engine.js'),d=read('public/design/index.html'),i=read('src/index.js');assert.match(e,/retryMode=retryNumber===1\?'alternate':retryNumber===2\?'deep':'snapshot'/);assert.match(e,/avoidRepeat:true/);assert.match(e,/retryMode==='alternate'/);assert.match(e,/retryMode==='deep'/);assert.match(e,/retryMode==='snapshot'/);assert.match(d,/rota de leitura diferente/);assert.match(i,/adaptiveRetryV0975/);console.log('RONDA ONE v0.9.7.5 Adaptive Retry No Repeat: OK');