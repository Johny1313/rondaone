import assert from 'node:assert/strict';
import fs from 'node:fs';
const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
const pkg=JSON.parse(read('package.json'));
const design=read('public/design/index.html');
const worker=read('src/ronda/v285/index.js');
const platform=read('src/index.js');
assert.match(pkg.version,/^0\.9\.7(?:\.\d+)+$/);
assert.match(platform,/mandatorySlideCountV09721/);
assert.match(platform,/requiredBeforeProduction:true/);
assert.match(design,/Quantos slides terá este carrossel\?/);
assert.match(design,/requestCarouselSlideCount/);
assert.match(design,/Nenhuma matéria será lida e nenhuma IA será acionada antes de você confirmar/);
assert.match(design,/const slideCount=await requestCarouselSlideCount\(\)/);
assert.match(design,/sourceType:'url',url:articleUrl,slideCount/);
assert.match(design,/sourceType:'topic',topicId,runId:runId\|\|null,slideCount/);
assert.match(design,/min="3" max="15"/);
assert.match(worker,/Escolha quantos slides terá o carrossel antes de iniciar a produção/);
assert.match(worker,/validateSlideCount\(body\.slideCount\)/);
console.log('RONDA ONE v0.9.7.2.1 Mandatory Slide Count: OK');
