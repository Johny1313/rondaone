import assert from 'node:assert/strict';
import fs from 'node:fs';
const design=fs.readFileSync('public/design/index.html','utf8');
const engine=fs.readFileSync('src/production/engine.js','utf8');
const worker=fs.readFileSync('src/ronda/v285/index.js','utf8');
assert.match(design,/id="directArticleRetry"/);
assert.match(design,/Tentar novamente/);
assert.match(design,/retryLastFormaProduction/);
assert.match(design,/registerProductionFailure/);
assert.match(design,/same job|mesmo job/i);
assert.match(design,/\/api\/production\/jobs\/\$\{encodeURIComponent\(jobId\)\}\/retry/);
assert.match(engine,/Nova tentativa solicitada pelo operador/);
assert.match(engine,/sameJob:true/);
assert.match(engine,/runDirectProductionRecovery\(env,id,\{stage:"generating",ctx\}\)/);
assert.match(engine,/launchInteractiveProduction\(env,id,\{force:true,ctx\}\)/);
assert.doesNotMatch(engine,/if\(stage==="generate"&&job\.evidenceId\).*queueForCarousel/s);
assert.match(worker,/productionRetryMatch/);
console.log('v0.9.7.4.2 Retry UX + Same Job Recovery OK');
