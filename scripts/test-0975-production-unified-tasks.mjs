import assert from 'node:assert/strict';
import fs from 'node:fs';
const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
const db=read('src/ronda/v285/database.js');
const design=read('public/design/index.html');
const board=read('public/ronda/production-view.js');
const engine=read('src/production/engine.js');

assert.match(db,/task_title/);
assert.match(db,/task_origin/);
assert.match(db,/comment_text/);
assert.match(db,/register_forma_task/);
assert.match(db,/set_comment/);
assert.match(design,/function productionTaskId/);
assert.match(design,/registerFormaTask/);
assert.match(design,/project\.productionTaskId=directProductionTaskId\(\)/);
assert.match(design,/action:'mark_review'/);
assert.match(board,/Gerado por/);
assert.match(board,/Comentários da tarefa/);
assert.match(board,/set_comment/);
assert.match(board,/\['production','Produção'\]/);
assert.match(board,/\['review','Aprovação'\]/);
assert.match(board,/\['finalization','Finalização'\]/);
assert.match(board,/\['completed','Concluído'\]/);
assert.match(design,/startFormaProduction\(\{sourceType:'topic',topicId,runId:runId\|\|null,slideCount\}\)/);
assert.doesNotMatch(design,/sourceType:'event',eventId:editorialEventId/);
assert.match(engine,/scrapeTopicToEvidence\(topic/);
console.log('RONDA ONE v0.9.7.5 Produção unificada + tarefas FORMA + comentários: OK');
