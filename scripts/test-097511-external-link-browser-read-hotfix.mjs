import fs from "node:fs";
import assert from "node:assert/strict";
const engine=fs.readFileSync(new URL("../src/production/engine.js",import.meta.url),"utf8");
const pkg=JSON.parse(fs.readFileSync(new URL("../package.json",import.meta.url),"utf8"));
assert.equal(pkg.version,"0.9.7.5.11");
assert.match(engine,/waitUntil,timeout:safeBudget/);
assert.match(engine,/run\("networkidle2"/);
assert.match(engine,/run\("domcontentloaded"/);
assert.match(engine,/typeof response\.ok==="boolean"&&!response\.ok/);
assert.match(engine,/parsed\?\.success===false/);
assert.match(engine,/BROWSER_READ_TIMEOUT_MS\)\|\|9_500/);
console.log("v0.9.7.5.11 external-link Browser Run hotfix: OK");
