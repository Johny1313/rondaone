RONDA ONE CLOUD v0.7.9 — ARQUIVO COMPLETO

Este diretório já contém o projeto completo atualizado.
Não execute os patches 0.7.8 ou 0.7.9 sobre ele.

Para atualizar o GitHub:
1. faça backup do repositório atual;
2. substitua o conteúdo do repositório pelos arquivos desta pasta;
3. preserve secrets/variáveis configurados diretamente no Cloudflare Dashboard;
4. faça commit/push;
5. execute npm install, npm run check e npm run verify:079;
6. faça npm run deploy, ou deixe a integração GitHub/Cloudflare executar o deploy.

Importante:
- o banco D1 não é apagado por esta atualização;
- Queues existentes continuam com os mesmos nomes;
- a IA de geração de imagem do FORMA DESIGN está desativada;
- o binding AI permanece porque o pipeline editorial/tradução existente pode usá-lo internamente;
- o carrossel possui fallback determinístico e não deve inventar fatos.
